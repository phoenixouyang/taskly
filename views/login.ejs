<!DOCTYPE html>
<html lang="en" data-theme="pastel">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/main.css">
        <title>Login</title>
    </head>
    <body class="min-h-screen flex items-center justify-center bg-neutral-content">
        <div class="card w-96 bg-base-100 shadow-xl p-6 space-y-6">
            <h2 class="card-title text-xl text-neutral mb-2 pb-2">Login</h2>
            <form action="/login" method="POST" class="space-y-4">
                <div>
                    <input type="text" name="username" placeholder="Username" required
                        class="w-full p-2 border border-[#aab8c2] rounded focus:border-[#1da1f2] outline-primary" />
                </div>
                <div>
                    <input type="password" name="password" placeholder="Password" required
                        class="w-full p-2 border border-[#aab8c2] rounded focus:border-[#1da1f2] outline-primary" />
                </div>
                <button class="btn rounded bg-primary w-full p-1" type="submit">Login</button>
                <a href="/register" class="link link-neutral">Don't have an account?</a>
            </form>
        </div>
    </body>
</html>